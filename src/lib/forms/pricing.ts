import type { PriceTable } from '@/types/forms/zoukoma';

/**
 * 増コマ（追加授業）の学年別単価。
 *
 * 値は2026年9月改定の料金表「追加授業（単コマ）」＝1対2・90分の単価。
 * ★単価の直書きはこのファイルだけにする。料金改定のときはここを直せば全部に効く。
 *
 * ここにあるのはあくまで**新しく作る申込期間の初期値**。実際に保護者へ提示される単価は
 * 期間ごとに form_periods.settings.price_table へ焼き付けられるので、ここを直しても
 * 過去の期間の単価は動かない（当時の価格のまま残る）。
 */
export const DEFAULT_GRADE_PRICES: PriceTable = {
  中1: 4380,
  中2: 4380,
  中3: 4540,
  高1: 4930,
  高2: 5250,
  高3: 5570,
};
