/**
 * 講師向け画面での生徒名表示（家モードの切替点）。
 *
 * 現在は素通し（フルネームをそのまま表示）。
 *
 * ★ 社内で最終判断待ち（正典 docs/teacher-home-mode-plan.md §1-6）:
 *   家モードの講師に /today・/my-schedule で生徒の実名を出してよいかは未確定。
 *   「中2英語」のような属性だけのマスク表示に倒す可能性がある。
 *   その判断が出たときに **この関数1つを変えるだけ** で済むように、家OKページの
 *   生徒名表示は必ずここを通すこと（呼び出し側に条件分岐を書かない）。
 *   マスクに倒す場合は、家モードか否かの引数をここに足して分岐させる。
 *
 * 適用範囲は家OKページ（/today・/my-schedule）のみ。教室限定ページは
 * そもそも家モードでは開けないので通していない。
 *
 * @param name 生徒の表示名（通常は「姓 名」）
 * @returns 講師に見せる文字列
 */
export function displayStudentNameForTeacher(name: string): string {
  return name;
}
