# 面談ワークスペースの組み替え（2026-09）

`/interview` の生徒画面を「**上＝話すこと、下＝材料**」に組み替える。
前身の台本の設計は `docs/interview-script-ai-plan.md`、③の定型は `docs/interview-timing-talks.md`。

## なぜ組み替えるか（2026-09-22・教室長の指摘）

- **話すことが材料の中に埋まっている。** いまは左440pxの列に「タスク → 面談記録 →（その中に）申し送り・話すこと・志望校・記録一覧」の入れ子。データと行動の区別が画面に無く、何をすればよいか分からない
- 成績の推移グラフが小さく、合計を0起点の棒で描くので +15点が誤差にしか見えない
- 面談記録は Notta の取り込みを生で貼っていて読めない。空の項目（「確認できませんでした」）が本文の半分を占める
- タスクはほとんど使っていないのに最上段にある

## 決めたこと

### 全体の並び（上から）

| 段  | 中身                                         | 幅             |
| --- | -------------------------------------------- | -------------- |
| 1   | ヘッダー（生徒の切替・氏名・最終面談・印刷） | 全幅           |
| 2   | 帯「話すこと」                               | 全幅           |
| 3   | **面談で話すこと**（InterviewScriptCard）    | **全幅**       |
| 4   | 帯「材料（記録）」                           | 全幅           |
| 5   | 成績 ｜ 面談記録                             | **半々**       |
| 6   | 進行表 ｜ 授業の様子                         | 半々（二次的） |
| 7   | 未完了の約束・タスク                         | 全幅・最下段   |

志望校の入力欄は成績の下（材料側）。④の「聞いて入れる」導線はそのまま。

### 面談で話すこと ―― 項目ごとに 左＝話すこと／右＝事実

> ★2026-09-23 に1列へ改めた（右の事実は各シーンの下に畳む）。いまの形は末尾の
> 「2026-09-23 整理（根拠を畳む・ひとこと・場面・言葉・Notta・検索）」を正とする。以下は当初の決定の記録。

7シーンを①→⑦の順に**縦に**並べ、各シーンを2列にする。**左を上から追えば面談が進む。右はその根拠。**

| 左（話すこと）                                                                       | 右（事実）                                                                         |
| ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- |
| 定型行（①③⑥⑦）／聞く（チェック）／AIの見えること／⑤のつながり／見せる物／③の想定問答 | 伝える行（数字）／目標の達成度／志望校との差／入試までの日数・出願〆切／申込の状況 |

- 左は主役（13px・濃い）。右は少し静か（12.5px・薄い背景）
- 列の見出し「話すこと」「事実（記録から）」はシーン一覧の先頭に1回だけ
- ①⑦は右が空。**空のまま**にする（非対称のほうが「ここは言うだけ」と分かる）
- 印刷シートは今回いじらない（すでに2列の紙）

### 右（事実）から材料へ飛ぶ

事実の行の見出し（「定期テスト」「内申」「模試」「進行表」「前回の面談から」）をクリックすると、
下段の該当カードへスクロールする（アンカー＋smooth scroll）。**数字を見て「元を見たい」を1クリックにする。**

### 成績の推移

- 棒→**折れ線**。科目ごとの線＋ドット、凡例
- **縦軸を絞る**（データの最小−5〜最大+5、10刻みに丸める）。注記「縦軸は◯〜◯に絞って表示」を必ず出す
- 合計はスケールが違うので線に混ぜず、表の「5科合計」行と「前回比」列で見せる
- 表に「前回比」列を足す（＋は緑・−は赤）
- rechartsは入れない（前身の判断を維持）。インラインSVG

### 面談記録の読みやすさ

Notta 取り込みを構造化して出す。

- 【タイトル】【録音日時】【音声URL】【参加者】は本文に出さない。音声は右下の「録音を開く ↗」
- 「--- Notta 要約 ---」以降の「■ 見出し」を小見出し、「・」を箇条書きに
- ★**空の項目は畳む。** 箇条書きが「見つかりませんでした」「確認できませんでした」しか無い見出しは、
  末尾に「記載なし：前回の確認・塾からの報告」と1行にまとめる。対象は Notta 由来の定型文だけで、
  手入力の短い記録はそのまま出す
- 既存の `stripNottaMeta` / `extractHandover`（interview.shared.ts）を土台に、パーサを1つ足す

## 中身の追加（第2段）

### ② ヒアリング ―― 「前回こう言ってましたが、その後どうですか」

せっかく来てもらうので、**家庭で見えないことと、良い報告**をここに集める。

右（事実）:

- 前回の面談から ―― 日付・経過日数（既存）
- **前回の約束** ―― 未完了のタスク（`interview_type='task'`）を1件1行。「英語ワークP10まで（未完了）」
- **前回の要望** ―― 直近の面談記録の「保護者からの要望」「次回への申し送り」の箇条書き
- 授業の様子 ―― 引継ぎ N件（**直近3件**を出す。いまは1件）。
  ★**同じ講師・同じ引継ぎ文が続く行は、いちばん新しい1件に畳む**（引継ぎは単元に付くので、
  同じ単元を数回に分けて進めると全部の回に同じ文が乗る。実物で 9/15 と 9/11 が同文だった）。
  ④のLIVE進行表も同じ（`dedupeConsecutiveLessonLines` / `dedupeConsecutiveHandovers`）
- **並びは面談で話す順**：前回の面談から → 前回の約束 → 前回の要望 → 授業の様子 → 宿題・遅刻 →
  目標の達成度。★表示の順であって、AIへ送る順（`BRIEF_SECTIONS`）は変えない

左（話すこと）:

- ★**約束・要望は「聞く」か「報告する」かを中身で決める**（2026-09-23・教室長の指摘）。
  一律に「その後どうですか」と聞いていたが、**保護者からの要望**（「英語の長文を増やしてほしい」）は
  頼まれた側が塾なので、こちらが**対応を報告する**もの。聞き返すと「前に頼んだのに何もしていないのか」になる。
  一方「慶應を含めて最後まで検討」は家庭に**聞く**。「英語ワークP10まで」は記録から追えることもある
- 振り分けは**AIが1件ごとに行う**（`followUps`: `{ item, kind: 'report' | 'ask', text }` を最大6件）。
  - `item` は渡した約束・要望の文そのもの。1字でも違えばパーサが捨てる（AIが言い換えた約束を台本に載せない）
  - `kind='report'`＝記録（引継ぎ・成績・授業の様子）から追えた／塾がしたことを伝えられる。本文が空なら捨てる
  - `kind='ask'`＝記録に無いので家庭に聞く。`text` が空なら画面が「前回の『◯◯』はその後どうですか」を出す
  - `text` にも**数字は書かせない**（seen・bridge と同じ縛り。上限も `MAX_SEEN_LENGTH` と同じ）
- ★**AIが使えない日の受け皿は出どころで振る**（`buildPreviousCommitmentLines` の `items[].source`）。
  「保護者からの要望」の見出しから来たもの → **話す**「報告 ―― 前回の要望『◯◯』への対応を伝える」（中身は教室長が口頭で埋める）。
  約束（未完了タスク）・「次回への申し送り」・「今後の方針」 → 従来どおり**聞く**
- 報告の行はAIが書いた文なので、着眼点（見えること）と同じ枠・同じアイコンで出す。
  ただし**直せない**（着眼点と違い、事実の報告をその場で書き換える場面が無いため。要れば後で足す）
- 印刷シートも同じ規則で振る（報告＝「話すこと」の箇条書き／聞く＝チェック欄）
- AI（見えること）に**授業中のポジティブな報告**を書かせる。プロンプトの lessons の指示に
  「できるようになったこと・授業中の発言・態度の変化を、保護者に伝える良い報告として書く。家庭では見えないことを優先する」を足す
- AI の lastInterview の指示に「前回の約束・要望に対して、その後の記録（引継ぎ・成績）から追えることがあれば書く」を足す

### ④ 現状の確認 ―― 進行表は LIVE の教材だけ、引継ぎのテキストが主役

- 出すのは **科目ごとに1冊の LIVE 教材**（`pickLiveTextbookIds` と同じ判定＝最終利用日が最新）。4冊全部は出さない
- 1冊につき：教材名（科目）・最終記入日・**直近3回の 日付 単元（講師）：引継ぎ**・次にやる単元
- 進捗%は出さない（数字の話は成績でする）

志望校（登録がある生徒）:

- 右：第1〜3志望ごとに「学校名（学科）―― めやす 内申◯・偏差値◯ ／ 本人との差 ／ 沿線」。
  ★最寄駅は出さない（直線距離で使えない。`docs/data/README.md`）。沿線（`access_lines`）まで
- 左：聞く「志望校の見学・説明会に行ったか」「併願の私立は決まっているか」
- AI が④で志望校に触れられるよう、志望校との差の行を `score` セクションの current に含めて送る

### ⑤ プラン提示 ―― 今期の提案と、これまでの講習の履歴

★**材料は提案書（`seasonal_proposals`）。`koushu_enrollments` ではない。**
本番の `koushu_enrollments` は0行で、実際の講習は提案書（生徒×テキスト×期）で回っている
（2026夏期だけで approved 575行・232名）。ここを読み違えていたため、全生徒が「講習: 申込なし」と
出ていた。`koushu_enrollments` は2027-02公開のWeb申込の入力源なので消さず、同じ期のバケットに
合流させる（`mergeKoushuSeasons`）。

- 期は **年度×季節**。年度は4月始まりで、1〜3月は前年度（`koushuFiscalYear`）
- 右・今期：「提案 数学 8コマ・英語 6コマ（申込済）」。`status` が `sent` なら（提案中）、
  `draft` なら（下書き）。コマ数が1つも入っていなければ「提案あり・コマ未確定」
- 右・履歴：今期を除いて新しい順に最大4件「夏期 2026：数学 12コマ・英語 8コマ（申込）」。
  ★**approved の期だけ**（下書き・提案中のまま終わった過去の期はノイズ）。無ければ行を出さない
- ヘッダー帯の「講習: 」・⑤の見出しバッジ・⑥「申込の状況」は `summarizeCurrentKoushu` の
  1つの値を使う（3か所で食い違わせない）。今期が空でも「申込なし」で終わらせず
  「冬期 2026 は申込なし（直近 夏期 2026 98コマ・申込済）」と直近の期を添える
- AIへ送る `koushu` セクションの `current` にも今期の提案の行を載せる（`buildTellSections`）

## 教室長レビューの反映（2026-09-23）

### AIの文は枠の中でスクロールさせない

- 見えること（`SeenLine`）の textarea は**中身の高さに合わせて伸びる**（`useAutosizeTextarea`）。
  値が変わったとき・ウィンドウ幅が変わったとき（折り返しが変わる）に測り直す。直せるのはそのまま
- 以前は rows を字数で1〜2行に決め打ちしており、数百字の着眼点が枠内スクロールになっていた
- 報告の行（`ReportRow`）・つながり・つなげて見えることは素の段落なので、もともと全文が出る。印刷シートも全文

### ③時期の重要性は畳める（既定はたたむ）

- ★畳めるのは**③の左だけ**。③は学年×季節×都県の定型と想定問答で、どの生徒でも毎回同じ文。
  台本の中で一番長いのに、読み慣れた教室長には要らない。②④⑤は生徒ごとに中身が変わるので畳まない
- たたんだとき：左は「定型 12行・想定問答 8件を表示」のボタン1つ。右（入試まで・出願〆切）は出したまま
- 開閉はブラウザごとに覚える（localStorage `nest.interview.timingOpen`。読めなければたたむ）
- 印刷シートは常に全部出す

### ④の左で志望校について話す

右に「めやす 必要内申49（+4）・必要偏差値55（-1）」が出ていても、左に言うことが無く、志望校に触れずに終わっていた。
④の左の**先頭**に、志望校ごとの行をシステムが組む（`buildTargetSchoolTalkLines`。AIは使わない。
差はシステムの計算なので数字を使ってよい）。

- 内申：上回る「狛江：内申はめやすを4上回っている。推薦も一般も内申が武器になる」／
  届かない「〜届かない。当日の点で取り返す（換算内申1点は当日の素点で約3点ぶん）」／ちょうど
- 偏差値：上回る「偏差値もめやすを◯上回っている。このまま維持」／届かない「あと◯。次の模試で届く幅かを一緒に見る」
- 組み合わせ：両方プラス→聞く「第1志望として安全圏。上の学校を狙うかを聞く」／
  内申プラス・偏差値マイナス→聞く「推薦を受けるか聞く」／内申マイナス・偏差値プラス→「一般入試の当日点で勝負する形になる」
- マスタに当たらない・本人の内申も模試も無い →「めやすと比べる材料が無い（内申・模試を聞いて入れる）」
- ★差は右の志望校の行と**同じ関数**（`targetSchoolDiffs`）を通す。満点が65でない学校で内申の話をしない決まりも揃う
- ★**神奈川・都県不明の教室は中立な比較だけ**。換算内申の比（都立1020点方式の話）と「推薦」の言葉を出さない
  （神奈川の公立に東京の意味での推薦入試は無い）。比の文言は `scenes.ts` の `TOKYO_NAISHIN_POINT_WEIGHT` 1か所
- 印刷シートは④の右列の先頭に「話すこと」として同じ行を出す
- AIのプロンプト（score）にも「志望校には必ず触れる。推薦・一般のどちらが向くか、内申と当日点のどちらを伸ばすか、
  次の模試で何を見るかを、学校名を出して書く。数字は書かない」を足した

### 前回の要望に Notta の「無い」と論評を拾わない

- `parseNottaSummary` は「確認できません」「発言はありません」「見当たりません」「ありませんでした」「特になし」等で
  **終わる箇条書きを1件ずつ落とす**（以前は見出しの中身が全部それのときだけ畳んでいた）。全部落ちた見出しは従来どおり「記載なし」
- ②の「前回の要望」に拾うときだけ、要望への論評（頭が「要望の強さ」「要望は」「要望としては」、
  または「というより」「レベルです」を含む）を外す。面談記録カードには残す
- ★判定は狭くしてある。要望を取りこぼすほうが害が大きいので、広げるときは実物の文で確かめてから

## 神奈川県立の志望校

高校マスタに神奈川の公立199行（新教育研究協会「合格基準一覧表 2026年度」・基準内申は135点満点）が
入ったので、志望校パネルと④を神奈川にも対応させた。

- **検索**：都県で絞らない（東京の教室の生徒が神奈川県立を志望することもある）。教室の都県
  （`regionOfSchool`）の学校を先に並べる。候補には「東京」「神奈川」を添える
- **本人の内申（135点満点）**＝中2学年末の9教科合計×1＋中3の9教科合計×2（`calcKanagawaNaishin135`）
  - 中2学年末＝`year_end`、2期制は後期 `second`。中3は2学期 `term2`／後期 `second` を最優先
    （学年末 `year_end` は入試のあとの評定なので2学期より後ろ）。1学期 `term1`／前期 `first` しか無ければ**仮計算**
  - 中2学年末が無い・9教科のどれかが欠ける → 出さない（推測で埋めない）
  - 既存の `calcKanagawaNaishin`（1行・45点満点。成績一覧の列）は変えていない
- ★**どちらの内申で比べるかは学校の満点で決める。教室の都県では決めない。**
  65＝都立の換算内申、135＝神奈川の中2＋中3×2、75・52＝差を出さない（従来どおり）。
  神奈川で内申欄が空の16校は満点も空なので、都県で135とみなす（`displayNaishinMax`）
- ④の右：「めやす 基準内申107/135（本人 98・-9）・基準偏差値60（-2）（合格基準一覧表 2026年度／原本との突き合わせは未了）」。
  仮計算なら「（中3は1学期の評定で仮計算）」を添える。語は資料どおり「基準〜」（都立は「必要〜」のまま）。
  「合格可能性60%の位置」はVもぎの定義なので神奈川には付けない。沿線は入っていれば出す。最寄駅は出さない（従来どおり）
- ④の左：神奈川向けの中立な文に本物の数字が入る。仮計算なら内申の行に「（仮計算）」。
  東京の教室でも志望校が神奈川県立なら、推薦・換算内申の比は出さない
- AIのプロンプトは変えていない（score の現状に上の行がそのまま入る。神奈川の教室では従来どおり推薦・換算内申の話を止めてある）
- ★2026-09-23 時点で、緑園都市校の中3に「中2学年末＋中3」の両方がそろった生徒はいない。本人の数字はまだ誰にも出ず、
  基準内申だけが出る。中3の1学期・2学期の評定が入り次第出る

## ②ヒアリングを 振り返り／学校／塾／家庭 に分ける（2026-09-23）

②は材料が多く、1列に並べると「いま何の話をしているか」が見えなくなっていた（前回の約束の次に部活の話、
その次に宿題の回数…）。保護者との話の順＝**振り返り → 学校 → 塾 → 家庭**に切り、小見出し
（13px太字・左に細い線）の下にいつもの左（話すこと）／右（事実）を置く。モックは承認済み。

| 小見出し | 左（話すこと）                                                                                                                            | 右（事実）                                               |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 振り返り | 前回の約束・要望への「報告」／「その後どうですか」＋ `lastInterview` の見えること                                                         | 前回の面談から（申し送り）・前回の約束・前回の要望・方針 |
| 学校     | 聞く「学校の授業と宿題の進み具合」「学校の面談で言われたこと」「学校生活の様子（部活動の引退後…）」＋目標の達成度の「結果を聞いて入れる」 | 目標の達成度                                             |
| 塾       | `lessons`・`discipline` の見えること（家庭では見えない塾での様子）                                                                        | 授業の様子・宿題・遅刻                                   |
| 家庭     | `parent` の見えること＋聞く「家庭学習の様子…」「次の定期テスト・模試の目標を決める」                                                      | `parent`（保護者とのやりとり）の行。無ければ空           |

- ★**振り分けはデータで持つ**（`scenes.ts`）。聞くことは `HEARING_GROUPS`、AIセクション→小見出しは
  `HEARING_GROUP_OF_SECTION`。画面（`InterviewScriptCard`）と紙（`InterviewPrintSheet`）はこれを読むだけで、
  JSXの中で決め打ちしない。`ASK_LINES.hearing`（平たい一覧）は廃止した（テストで戻らないようにしてある）
- `HEARING_GROUP_OF_SECTION` の型は②に置く4セクションに絞った。②にセクションを足して振り分けを
  書き忘れると型エラーになり、テストも落ちる（黙ってどの小見出しにも出ない、を防ぐ）
- `SCENE_OF_SECTION`（どのシーンに置くか）は変えていない。AIに投げる単位・順も変えていない
- 左右とも空の小見出しは出さない。片方だけ空なら空のまま（事実が無いことに埋め草を置かない）
- ★`parent` の現状の行は「家庭」の右に出す。モックでは「家庭」の右は空だったが、それは例の生徒に
  保護者とのやりとりが無かったから。行があるのに消すと、以前は②の右に出ていた事実が画面から消える
- 印刷シートも同じ振り分け・同じ順。小見出しは9.5pxの1行にとどめ、A4 1枚に収めるためメモ欄を
  300px→250pxにした（罫線2本ぶん）

## 模試の志望校と合格可能性（2026-09-23）

模試（進研テスト・Vもぎ）の結果には生徒が書いた志望校（最大5校）と合格可能性が載っている。
取り込み画面（生徒一覧「模試結果集計」→「模試結果の一括取り込み」＝ `MockPasteImportModal`）は
志望校名をプレビューに出すだけで、**合格可能性は読み捨て、志望校も保存していなかった**。

### 模試の志望校欄の形（教室長から受け取った実データ）

- 志望校名と合格可能性の対が最大5つ。**1〜3番目が公立、4〜5番目が私立**
- 空いた枠は半角スペース1つ。★**枠を詰めない**（詰めると私立が公立の枠に入り、第1志望の自動登録を誤る）
- 学校名は「学校－学科」（全角ダッシュ）か、学科なし（「若葉総合」「専修大附属」）
- 合格可能性は10〜95か `**`（判定不能）。★**`**` は NULL＋`possibility_unjudged` で持つ。0 にしない\*\*
  （0 と持つと「可能性ゼロ」と読まれ、次の模試と比べて「上がった」に見える）

### 保存（`assessment_target_schools`）

- 模試1回（`assessments` の1行）に最大5行。`slot`（1〜5）・`is_public`（slot≤3）・
  `school_name_raw`（書かれたまま）・`high_school_id`（マスタに当たったときだけ）・`possibility`・`possibility_unjudged`
- `assessment_id` は ON DELETE CASCADE（模試の行を消したら志望校も消える）、`high_school_id` は SET NULL
  （マスタの版の入れ替えで模試の記録を道連れにしない。`student_target_schools` と同じ考え方）
- RLS は `student_target_schools` と同じ教室スコープ。`school_id` はトリガーで生徒の所属校に強制。
  anon / authenticated から REVOKE ALL したうえで authenticated に付け直す
- マイグレーション: `supabase/migrations/20260923140000_assessment_target_schools.sql`（★本番未適用）

### マスタへの当て（`matchMockSchoolName`・`src/lib/scores/mockSchools.ts`）

- 「－」で学校と学科に分け、冠（都立・県立・神奈川県立）と「高校」「高等学校」を外す。★「市立」は外さない
  （神奈川のマスタは「市立橘」のように名前に含む）。学科「普通」→ course `''`（普通科の本体）、末尾の「科」も外す
- (学校名, 学科) の組で当てる。★**学科が違う行には当てない**（別の学科のめやすを使わない）。
  学科なしの書き方は、本体（course `''`）→ 無ければその学校の行が1つだけのときだけ当てる
- 同名の学校（「多摩」は東京と神奈川の両方）は教室の都県を先に選ぶ
- 当てるのは公立の枠だけ。私立はマスタに無いので当たらないのが正常（`high_school_id` は NULL）

### 志望校の自動登録

- 取り込み画面のチェック「志望校が未登録の生徒には、模試の公立の志望校を第1〜3志望として登録する」（既定オン）。
  プレビューに「◯名に志望校が入ります」を出す
- ★**志望校が1件も無い生徒だけ**。取り込む直前にもう一度数え、1件でもあれば触らない（上書きしない）
- 公立の枠を順に rank 1..n。学校名はマスタに当たればマスタの名前（志望校パネルと同じ形）、
  当たらなければ模試に書かれたまま。理由は空（模試は理由を持たない。面談で聞いて入れる）

### 面談④に出す

- 右（事実）：`直近の模試（会場模試 10月） ―― 狛江 60%（前回 50%）・神代 20%・三田 判定なし／私立 専修大附属 70%`
  - 直近＝志望校が入っている模試のうち一番新しいもの（`listAssessments` の新しい順）。前回＝その1つ前の同じ学校
    （マスタに当たっていればIDで、無ければ短い名前で突き合わせる）
- 模試に書いた公立校が志望校に登録されていなければ、右に `★模試では 三田 も書いている（登録に無い）`、
  左の「聞く」に `模試で書いた 三田 は志望校に入れるか聞く`
  - ★志望校が1件も無い生徒には出さない。④に「志望校を聞いて入れる」が既に出ており、同じことを2回言わせない
- AIには「直近の模試」の行を `score` の現状に混ぜて渡す（合格可能性の流れに言葉で触れられるように）。
  プロンプトに「数字は書かない・判定なしを可能性ゼロと言い換えない」を足した。
  表示側は志望校の行と同じく `stripTargetSchoolFactLines` で外し、④の志望校ブロックとして別に出す
- 印刷シートの④にも同じ行を出す

## 2026-09-23 整理（根拠を畳む・ひとこと・場面・言葉・Notta・検索）

承認モック: `interview-layout-mock.html`（凡例 11〜14）。「話す前の確認」と「話している最中」を分け、
面談中は上から話すことだけを追えるようにする。あわせて、AIの文を「読む下書き」から
「そのまま言える話し言葉」まで一歩寄せる。

### 1列にして、根拠（記録）は畳む（凡例 11）

- シーンの左右2列をやめ、1列（本文幅は 880px まで）。2列だと目が左右を往復し、いま何を言うかが追えなかった
  （★同日、広い画面だけ「左＝話すこと／右＝聞くこと・根拠」の2列にした。下の「広い画面は…」を参照）
- 以前の右の列（事実）は、各シーン・②の各小見出しの一番下に「▸ 根拠（記録）N件」として畳む。
  押すとその場で開き、中身は以前の右の列と同じ静かな枠。見出しを押して材料カードへ飛ぶのもそのまま
- カード右上の「根拠をすべて開く」で全部を開閉。ブラウザごとに覚える（localStorage `nest.interview.factsOpen`、
  既定はたたむ）。個別の開閉は覚えない（その場の確認用。全体の切り替えを押すと個別の開閉は捨てる）
- ③の「入試まであと◯日」は、根拠を畳んでも見えるように③の見出しの横にも出す
- ③の定型の開閉（`nest.interview.timingOpen`）はこれとは別物のまま

### 話す → 聞くの順（凡例 12）

各シーン・各小見出しの中は必ず (a) ひとこと → (b) 話すこと → (c) 聞くこと → (d) 根拠 の順。
聞くこと（チェックの行）は小さな見出し「聞くこと」の下に最後にまとめ、話す行の間に挟まない
（④の志望校の「推薦を受けるか聞く」も聞くことへ回した）。

### ひとこと（AI・凡例 13）

- AIの出力に `openers` を足した。置き場所は intro／review・school・juku・home（②の小見出し）／timing／status／plan。
  apply・closing は定型の事務連絡なので持たない
- 教室長が保護者にそのまま言う1文。ねぎらい・気持ちの受け止めが先、それから話題。丁寧語。
  生徒は名（下の名前）＋さん。名はサーバーが `students.first_name` から読んで渡す（クライアントの言い値にしない）
- ★数字を書かせない。seen／thread／bridge の「数字なし」はプロンプトの指示だけだが、
  ひとことは保護者にそのまま言う文で画面では直せないので、パーサでも弾く（`containsDigit`。漢数字は見ない）
- 60字超・知らない key は捨てる。plan は講習の提案（koushu）を渡していなければ捨てる（bridge と同じ理由）
- 見た目は着眼点（ink の枠）と分ける：14px・太字にしない・地は surface-hover・左に warning の線・小さな札「ひとこと」。
  暖色のトークンが無いので新しい色は足していない
- AIが使えない日は出さない（決まり文句で埋めない）

### 場面（AI・凡例 13）

- AIの出力に `episodes: { lesson, text }[]`（最大2件）を足した。lesson は【lessons】の行番号（1始まり）で、
  `briefUserText` が引継ぎの行にだけ `1. ` `2. ` と番号を振って渡す
- ★日付と講師はAIに書かせない。サーバーが番号で引継ぎの行を引き、`M/D` と講師の姓を付けて返す
  （`parseLessonLineHead`）。範囲外・重複・数字入り・70字超は捨てる
- ②「塾」の話すことに「場面」の札つきで出す：「9/17の英語で…（内山先生）」
- 選ばせるのは家庭で見えない瞬間（自分から質問した・粘った・できるようになった）。事務連絡だけの引継ぎは選ばせない

### 「」の言葉をそのまま運ぶ

- `extractQuotedWords`（interview.shared.ts）が直近の面談記録（タスク以外）から、本人・保護者の「」の言葉を拾う（最大3件）
- 古い型：文中の「」のうち、直後が「と」か話し言葉の語尾で終わるものだけ。話し手は同じ文の中で「」より前の
  いちばん近い語（生徒・本人・お子さん／保護者・お母様・母…）。分からなければ null。3字未満・60字超・見出しと同じ語・重複は拾わない
- ②「振り返り」の話すことに「言葉」の札つきで出す：「前回、◯◯さんは「頑張ります」と話していました」／
  「前回、保護者の方は「…」とおっしゃっていました」／「前回の面談で「…」という言葉が出ていました」
- AIには lastInterview の現状の行に「前回の言葉: 「…」（生徒）」として渡し、プロンプトで「言い換えずにそのまま引用する」

### Nottaの要約の新しい型（教室長が 2026-09-23 に変更）

見出しは同じ（前回の確認／塾からの報告／保護者からの要望／相談事項／今後の方針／総合メモ）＋新しい見出し「印象に残った言葉」。
古い型の記録も読めるまま残す。

| 新しい型の決まり                                           | NESTでの扱い                                                                                                                                                                                                        |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 話題が出なかった見出しは「（なし）」1件                    | 「確認できませんでした」と同じく記載なしに畳む                                                                                                                                                                      |
| 「感情：」「受け止め：」で始まる判断の行                   | 面談記録カードには出す。②の要望・約束・方針には拾わない。「」の言葉も拾わない                                                                                                                                       |
| 相談事項の「生徒：」（本人の反応）                         | 表示だけ                                                                                                                                                                                                            |
| 保護者からの要望は1件1行、末尾に【相談】【要望】           | 印は②とAIに渡す本文から外す（tag として持つ。いまは画面に出さない）                                                                                                                                                 |
| 今後の方針は頭に「塾：」「家庭：」「生徒：」「次回確認：」 | 頭の語は本文から外す。AIが使えない日の振り分けは 塾＝報告／それ以外＝聞く。語尾の当て推量より優先。AIにも〔塾が動く〕などを本文の後ろに添えて渡す（本文は外したあとの文で、画面とサーバーで同じ `followUpItemKey`） |
| 印象に残った言葉：「頑張ります」（生徒）                   | 節があればこの節だけから言葉を拾い、話し手は末尾の（生徒）（保護者）から取る。節が（なし）なら他の節から推し量らない                                                                                                |

- Slack経由で届く要約は見出しの印が無く、箇条書きが「•」で、見出しが前の箇条書きの行末に続く
  （「前回の確認 • …\n• …‎ 塾からの報告 • 英語は…」。U+200E も混ざる）。`parseNottaSummary` は
  知っている見出し名の直後に「•」が来たとき・知っている見出し名だけの行を見出しとして割る（自由文を見出しと取り違えない）。
  取り込み時の整形（`formatNottaTranscript`）を通して保存済みの記録もこれで読める
- 取り込み時の整形の見出し一覧（`NOTTA_SECTION_HEADERS`）に「印象に残った言葉」を足した

### 面談記録：Nottaから取り込み＋検索（凡例 14）

- 面談記録カードの見出しに「Nottaから取り込み」。中身は生徒詳細「面談記録」タブと同じ `ImportNottaModal`、
  出し分けも同じ `permissions.canEditInterviews`。取り込んだら面談記録を読み直す（台本の前回の要望・言葉にもすぐ効く）
- その下に検索欄（「面談記録を検索（例：推薦、英検）」）。タイトル＋本文の部分一致で、全角半角・大文字小文字を区別しない
  （`matchesSearch`／`splitForHighlight`。1文字ずつ NFKC を掛けて元の位置に戻せるようにしている）。
  件数を「n件」で出し、当たった所を warning-subtle で塗る（Nottaの見出し・箇条書き・素の本文・前回の申し送り）。
  検索中は当たった記録を畳まずに全部出す。前回の申し送りのピンは絞り込まない

### 広い画面は 左＝話すこと／右＝聞くこと・根拠（2026-09-23 追記・教室長承認）

1列＋880px の上限では、広い画面でカードの右半分がまるごと空いていた。そこで lg（1024px）以上だけ、
シーン・②の各小見出しを2列の格子にする（`renderBlock`）。

- 左（約 1.7）＝(a) ひとこと・(b) 話すこと（定型・見えること・報告・場面・言葉・見せる・つながり・③の想定問答）
- 右（約 1）＝(c) 聞くこと → (d)「▸ 根拠（記録）N件」。開いた根拠も右の列の中に出す（全幅にしない）
- ★旧2列（左＝事実／右＝話す）とは分け方が違う。話す行は左の1列だけを上から追えばよく、
  チェックの行と事実は右に寄せる。「聞くことを話す行の間に挟まない」は変わらない
- 880px の上限は外した。左の1行の長さは列の比で抑える
- 右に置くものが無いシーン（①導入・⑦クロージングなど）は右の列を立てず、左を全幅にする。
  右だけあるときも右の列に置く（場所が毎回同じ方が探せる）
- lg 未満は従来どおり縦に積む（話すこと→聞くこと→根拠）。③の開閉・②の小見出し・根拠の開閉と
  「根拠をすべて開く」・材料へのジャンプ・見えることの編集は変えていない
- 凡例は画面の左→右の順（…見せる → 聞く → 根拠）に並べ替えた

### 「つなげて見えること」を①の上へ（2026-09-23 追記・教室長の指摘）

- AIの `thread`（材料をまたいだ見立て）を、カードの末尾から凡例の直下・①より上・全幅へ移した。
  末尾だと面談前に読まれずに終わっていた。面談全体の芯なので最初に読むものとして置く
- 見た目は info-subtle の地・見出し「つなげて見えること」（11px 太字）・本文 14px。枠線や強い色は足さない
- 空なら何も出さない。印刷シートの位置は変えていない（別の変更で扱う）

### 印刷シート

体裁（2列・A4 1枚）は変えない。前回の言葉は lastInterview の行として「前回の面談から」に乗る。
ひとこと・場面は紙には足さない（メモ欄を 250px まで削ってやっと1枚に収まっているため。面談中に画面で読むもの）。

## 変えないこと

- AIに投げるセクションの単位と、数字を書かせない原則
- ③の定型・想定問答の持ち方（`scenes.ts`）
- 印刷シートの体裁（A4縦1枚・2列）。★中身は画面と揃える。2026-09-23 の「報告／聞く」の
  振り分けだけは、画面と紙で違うことを書くわけにいかないので紙にも入れた

## 進め方

1. 第1段：レイアウト・推移グラフ・Notta構造化・タスク移動・事実からのジャンプ（PR）
2. 第2段：②④⑤の中身（PR）
3. 実機確認は 永山校の中3（東京）と 緑園都市校の中3（神奈川）で行う
