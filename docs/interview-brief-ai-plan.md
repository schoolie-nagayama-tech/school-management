# 面談の準備メモをAIに作らせる（設計書）

作成: 2026-09-03 ／ 更新: 2026-09-07 ／ 状態: **実装済み（PR #167）**
対象: `src/app/interview/`（面談ワークスペース）
モック: [AIに書かせる場所](https://claude.ai/code/artifact/b60623a6-6bcf-4068-89f5-21685579aac0) の「面談の準備メモ」タブ
前提: [docs/ai-platform-comparison.md](./ai-platform-comparison.md)（基盤＝Claude API）

---

## 0. 何をするか

保護者面談の直前に、その生徒についてシステムにあるデータを**総動員**して
**「報告事項」**を作る。セクションに分け、それぞれ**現状**と**データから見えること**を
1行ずつ並べる。

**★長いと結局読まれない。** 1セクション2行まで。全体でA5に収まる量。

## 1. 決めたこと（2026-09-03）

1. **すべてのデータを使う。** 成績・報告書・引継ぎ・宿題と遅刻・出欠・進行表・講習・
   保護者とのやりとり・前回の面談。
2. **セクションごとに「現状」＋「見えること」。** 3点に絞る旧案はやめた。
3. **簡潔に。** 1セクション1行ずつ、見えることは40字まで。
4. **★現状の行はシステムが組む。AIが書くのは「見えること」だけ。**
   数字をAIに触らせないのは他のAI機能と同じ考え（[[構想_AIは書かせず整える]]）。
   AIが数字を書き直しても画面に出ない構造にする。
5. **並び順はシステムが固定。** AIに順番を決めさせない。

## 2. セクション（固定・この順）

| # | セクション | 現状の行（システムが組む） | 材料 |
| --- | --- | --- | --- |
| 1 | 成績 | 直近の定期テスト（前回比）・通知表 | `assessments`＋`assessment_scores`（`computeScoreSummary`） |
| 2 | 授業の様子（報告書・引継ぎ） | 報告書の件数・直近の講評の引用・繰り返し出る引継ぎ | `class_reports.review_comment`、`student_progress.handover`、`progress_sessions.handover` |
| 3 | 宿題・遅刻・出欠 | 月次の宿題忘れ日数・遅刻日数、欠席・振替回数 | `progress_sessions`（`computeDisciplineMonthly`）、`schedule_entries.attendance_status`、`class_reports.homework_*` |
| 4 | 進度（進行表） | テキストごとの進捗%・学校進度との差・次の単元 | `summarizeTextbookDetail`、`student_progress.school_progress_date` |
| 5 | 講習 | 前回の提案コマ vs 受講コマ、今回の提案書の状態 | `koushu_enrollments`、`seasonal_proposals` |
| 6 | 保護者とのやりとり | 件数・直近の連絡の要旨・返信待ちの有無 | `chat_threads`＋`chat_messages` |
| 7 | 前回の面談から | 日付・経過日数・申し送り | `student_interviews`（`extractHandover`） |

データが無いセクションは**行ごと出さない**（「記録なし」を並べない）。

## 3. AIの出力

```json
{
  "sections": [{ "key": "score", "seen": "下がったのは英語だけ。数学は2回続けて上がっている", "sign": "warn" }],
  "thread": "英語だけ、成績・宿題・出欠・家庭の話がすべて同じ方向を向いている。",
  "talk": [{ "text": "数学が下がった理由は計算ミス。解き方は分かっている", "basis": "lessons" }]
}
```

- `seen`: そのセクションから見えること。**1文・40字まで**。無ければ空文字（無理に書かない）。
- `sign`: `warn`（注意して話す）／`good`（伝えたい良い話）／空。画面では左の色線になるだけ。
- `thread`: **複数のセクションをつなげて初めて見えること**を1文。無ければ空。
- `talk`: **面談で順に話す項目、3〜5個**（各60字・根拠のセクション key 付き）。悪い話だけにせず、良い話を1つ入れる（2026-09-07 ユーザー指示。旧案の `ask`＝「保護者に聞くこと」と「気になっていること」の入力欄は削った。押すだけにする）。

## 4. AIに守らせること（プロンプトに固定）

- 【現状】と【データ】に書かれた数字と事実だけから読む。**無いことを足さない。数字を書き直さない。**
- 悪い話だけにしない。良い方向のものは良いと書く。
- 見えることが無いセクションは空にする。
- 内部メモなので保護者向けの言い回しにしなくてよい。

## 5. 画面

- 面談ワークスペース（`/interview`）の左カラム、「前回の申し送り」の下に**「報告事項」カード**を置く。
  すでに成績・進行表・宿題と遅刻のパネルが同じ画面にあるので、材料集めは**追加の読み込みがほぼ無い**。
- 入力欄は無し。ボタン1つ（2026-09-07 に簡素化）。
- 出てきた文は編集できる。印刷シート（`InterviewPrintSheet.tsx`）にも同じカードを出す。

## 6. 材料のうち、面談ワークスペースがまだ読んでいないもの

面談ワークスペースは 成績・進行表・宿題と遅刻（5か月）・面談記録・通塾日程・講習申込 をすでに読んでいる。
足りないのは次の2つだけ。

| 材料 | 読み方 |
| --- | --- |
| 出欠・振替 | **座席表に記録がある**: `schedule_entries.attendance_status`（`present / absent / late`、座席表のセルから記録）。生徒×期間で数える1クエリを足すだけ（`attendance.ts` は講師の勤怠なので使わない） |
| 保護者とのやりとり | `chat_threads` → `chat_messages` を生徒で引く。直近の要旨と返信待ちの有無 |

報告書の講評（`class_reports.review_comment`）も面談ワークスペースは読んでいないが、
`getApprovedReportsByStudent` がある。

## 7. 残っていること

- 「現状」の行を組むテンプレート関数（セクションごと・データが無ければ null を返す）。
- 生徒の出欠集計関数。
- 面談ワークスペースへのカード追加＋印刷シート。
- `docs/legal/privacy-policy.md` はリーガルチェック中のため改訂しない（2026-09-03）。チェック後に第5条へ Anthropic を追記してから公開。

## 8. 実装で決めたこと（2026-09-07・PR #167）

- **現状の行はクライアント（面談画面）が組んで送る。** 面談画面がすでに読んでいるデータ（成績・進行表・宿題遅刻・講習・前回面談）を二重に読まない。サーバーが足すのは**引継ぎ直近20件**と**保護者チャット直近10件**だけ。
- 引継ぎの取り方は「これまでの引継ぎをまとめる」（`/api/ai/handover/digest`）と同じ。栓も同じ `student_digest`。
- 「保護者と」の直近1件は `sender_kind` に従って「保護者」「教室」を出し分ける（直近が教室側の連絡のとき、保護者の言葉として読ませない）。日付はJST換算。
- 宿題・遅刻の行は「授業N日」を分母として添える（分母が無いと重さを読み違える）。
- 出欠（座席表の出席記録）は本番でほぼ空なので、セクションに含めていない。
- 保存しない。印刷シートには同じ内容を簡素に出す。
